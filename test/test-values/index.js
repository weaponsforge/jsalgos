const formatDuration = require('./formatduration-test')
const mixedFraction = require('./mixedfraction-test')
const parseStringInt = require('./parsestringint-test')

module.exports = {
  formatDuration,
  mixedFraction,
  parseStringInt
}